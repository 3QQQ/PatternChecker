pluginManagement {
    repositories {
        maven {
            name = 'LocalMirror'
            url = uri("${rootDir}/.local-repo")
        }
        gradlePluginPortal()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}
